{
  "name": "auto-closer",
  "version": "2.2.0",
  "description": "An Chrome extension that just close the tabs you don't need.",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.0.8",
  "scripts": {
    "watch": "esbuild src/contentScript.js src/background.js src/options.js --bundle --outdir=build --watch",
    "build": "rm -rf build && cp -r public build && esbuild src/contentScript.js src/background.js src/options.js --bundle --minify --outdir=build"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
